meta {
  name: POST-tokens-validate
  type: http
  seq: 5
}

post {
  url: {{API_SRC}}/tokens/validate
  body: json
  auth: none
}

body:json {
  {
    "token": "{{accessToken}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
